{
    "name": "letra-a-letra",
    "version": "1.0.0-alpha",
    "description": "A fast-paced multiplayer game blending word search and battleship, where players race to find letters while sabotaging their opponents.",
    "scripts": {
        "installAll": "npm install && npm install --prefix api && npm install --prefix ui && node -e \"require('clear')()\"",
        "start": "concurrently -n SERVER,UI -c blue,green \"npm run dev --prefix api\" \"npm run dev --prefix ui\""
    },
    "devDependencies": {
        "clear": "^0.1.0",
        "concurrently": "^9.2.1"
    }
}